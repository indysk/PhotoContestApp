<h1>コンテスト詳細ページ</h1>
<h3><%= @contest.name %></h3>
<p>作成者：<%= link_to @contest.user.name, @contest.user %></p>
<%= link_to  "写真を投稿", new_contest_photo_path(@contest) %>
<%= link_to  "前のページに戻る", :back %>
<%= will_paginate @photos, :previous_label => ' &lt 前へ', :next_label => '次へ &gt' %>
<div class="img_list">
  <%= render partial: 'photos/photo', collection: @photos %>
</div>
<%= will_paginate @photos, :previous_label => ' &lt 前へ', :next_label => '次へ &gt' %>
